## PolUni RAD-on ETL

A Java terminal application that extracts Polish university and institution data from RAD-on / POL-on APIs, applies user-selected filters, and exports institution metadata and student count statistics into CSV files.